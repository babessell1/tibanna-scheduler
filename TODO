postrun organizer:
    read outputs in s3
    write file of failures
        reclaimed
        md5 fail
        other
    delete successful
    write successful

downloader
    run postrun organizer first?

scheduler
    ensure no successful outputs are redone
    handle assigning instance types base on total samples

    export PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring

scale scheduler > local index

bug arthur
snd10000
snd10001

downstream
    df
        apoe4
        race
        cohort
        sex
        etc


